@extends('layouts.app')
@section('content')
<div x-data="{formOpen: false, edit: null}"><div class="flex justify-between items-center mb-5"><div><h1 class="text-2xl font-bold">Kategori</h1><p class="text-sm text-gray-500">Kelola pengelompokan barang toko.</p></div><button @click="edit = null; formOpen = true" class="bg-blue-600 text-white px-4 py-2 rounded-lg"><i class="fas fa-plus mr-2"></i>Tambah Kategori</button></div>@if(session('success'))<div class="bg-green-100 text-green-700 p-3 rounded mb-4">{{ session('success') }}</div>@endif @if($errors->any())<div class="bg-red-100 text-red-700 p-3 rounded mb-4">{{ $errors->first() }}</div>@endif<div class="bg-white rounded-xl shadow-sm border"><table class="w-full text-sm"><thead class="bg-gray-50 text-left"><tr><th class="p-4">Nama Kategori</th><th class="p-4">Deskripsi</th><th class="p-4">Jumlah Produk</th><th class="p-4">Aksi</th></tr></thead><tbody>@foreach($categories as $category)<tr class="border-t"><td class="p-4 font-semibold">{{ $category->name }}</td><td class="p-4">{{ $category->description ?: '-' }}</td><td class="p-4">{{ $category->products_count }}</td><td class="p-4"><button @click='edit = @json($category); formOpen = true' class="text-blue-600 mr-3"><i class="fas fa-edit"></i></button><form class="inline" method="POST" action="{{ route('categories.destroy', $category) }}" onsubmit="return confirm('Hapus kategori ini?')">@csrf @method('DELETE')<button class="text-red-600"><i class="fas fa-trash"></i></button></form></td></tr>@endforeach</tbody></table></div><div x-show="formOpen" x-cloak class="fixed inset-0 z-50 bg-black/40 flex items-center justify-center p-4"><div class="bg-white rounded-xl max-w-lg w-full p-6"><div class="flex justify-between"><h2 class="text-xl font-bold" x-text="edit ? 'Edit Kategori' : 'Tambah Kategori'"></h2><button @click="formOpen = false">&times;</button></div><form class="mt-5 space-y-4" :action="edit ? `/categories/${edit.id}` : '{{ route('categories.store') }}'" method="POST">@csrf <template x-if="edit"><input type="hidden" name="_method" value="PUT"></template><div><label>Nama Kategori</label><input name="name" required class="w-full border rounded p-2" :value="edit?.name"></div><div><label>Deskripsi</label><textarea name="description" class="w-full border rounded p-2" x-text="edit?.description"></textarea></div><button class="w-full bg-blue-600 text-white rounded py-2">Simpan</button></form></div></div></div>
@endsection